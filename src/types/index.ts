export interface Feature {
  id: string;
  icon: string;
  title: string;
  description: string;
}

export interface Discipline {
  id: string;
  label: string;
}

export interface NavLink {
  href: string;
  label: string;
}
